
  # EduChronix UI/UX Prototype

  This is a code bundle for EduChronix UI/UX Prototype. The original project is available at https://www.figma.com/design/XlcIlTmwelfiGutkaNNAcq/EduChronix-UI-UX-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  