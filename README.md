
  # EduChronix UI/UX Prototype

  This is a code bundle for EduChronix UI/UX Prototype.
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
